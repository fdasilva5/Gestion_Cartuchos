import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const RecargasPageLayout = (props: Props) => {
  return (
    <><Outlet /></>
  );
};

export default RecargasPageLayout;