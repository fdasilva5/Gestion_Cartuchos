import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const ImpresoraPageLayout = (props: Props) => {
  return (
    <><Outlet /></>
  );
};

export default ImpresoraPageLayout;