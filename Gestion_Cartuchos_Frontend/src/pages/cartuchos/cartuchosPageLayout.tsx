import React from 'react'
import { Outlet } from 'react-router-dom';

type Props = {};

const cartuchosPageLayout = (props: Props) => {
  return (
    <><Outlet /></>
  );
};

export default cartuchosPageLayout